export const KEYSTONE_MIN_LEVEL = 2;
export const KEYSTONE_MAX_LEVEL = 30;

/** Default keystone range applied when registering from a Blizzard character
 *  (no manual key input). Mirrors the manual form's initial 2–15 default. */
export const KEYSTONE_DEFAULT_MIN = KEYSTONE_MIN_LEVEL;
export const KEYSTONE_DEFAULT_MAX = 15;
