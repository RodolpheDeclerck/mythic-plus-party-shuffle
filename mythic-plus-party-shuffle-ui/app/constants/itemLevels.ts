// Retail / The War Within season range. Widened from the legacy 240–290 so the
// real Blizzard `average_item_level` (~600–680) is never clamped on auto-fill.
export const ITEM_LEVEL_MIN = 600;
export const ITEM_LEVEL_MAX = 700;

/** Optional color bands in roster tables (high → low within range). */
export const ITEM_LEVEL_TIER_HIGH = 675;
export const ITEM_LEVEL_TIER_MID = 650;
export const ITEM_LEVEL_TIER_LOW = 625;
